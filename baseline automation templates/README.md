Baseline automation templates are ready made templates custom build to assist with automation be in with any IAC tool. 
Replace ${} with the custom or required values. 
Note: Just be aware the templates might need tweaks according the vendor changes to their software versions. 